name: clean_archt_lint_example
description: Exemplo de uso do clean_arch_lint package.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  custom_lint: ^0.8.1
  clean_arch_lint:
    path: ../
